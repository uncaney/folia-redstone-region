<!-- Thanks for contributing! -->

## What does this change do?

<!-- Short summary of the change. -->

## Why?

<!-- The user-visible problem this solves. Link an issue if relevant. -->

## How was it tested?

- [ ] `./gradlew :plugin:build :test-plugin:build` passes locally
- [ ] `./test-harness/run-tests.sh` reports 14/14 cases passing
- [ ] If touching the redstone hot path: ran a perf comparison vs
      previous main on a 32×32 dust grid
- [ ] If touching i18n: both `lang/en.yml` and `lang/fr.yml` updated

## Anything reviewers should know?

<!-- API surface changes, config schema migrations, gotchas. -->
